import 'package:flutter/material.dart';

class Search_Flight extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Search Flight"),

      ),
      body: Column(children: [
        Container(),
        Container(),
      ],),
    );
  }
}
